import React from 'react';

function Navigation() {
    return (
        <nav style={{display: "flex", justifyContent: "flex-end"}}>
            <p className="black f3 link underline dim pa3 ma2 pointer">Sign in</p>
        </nav>
    )
}

export default Navigation;